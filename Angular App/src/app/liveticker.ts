export class Liveticker {

  constructor(public livetickerID: string, public authorID: string, public title: string, public status: string, public description: string, public state: string, public commentCount: number, public likeCount: number) {

  }
}
