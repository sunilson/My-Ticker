<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="46dp"
    android:layout_height="46dp"
  >

    <FrameLayout
        android:layout_width="@dimen/menu_item_ripple_size"
        android:layout_height="@dimen/menu_item_ripple_size"
        android:layout_centerInParent="true"
        android:background="?selectableItemBackgroundBorderless"
        />

    <ImageView
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_centerInParent="true"
        android:id="@+id/comment_icon_image"
        android:src="@drawable/ic_comment_white_24dp" />

    <FrameLayout
        android:id="@+id/comment_icon_red_circle"
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:layout_alignTop="@id/comment_icon_image"
        android:layout_alignRight="@id/comment_icon_image"
        android:layout_marginRight="-7dp"
        android:layout_marginTop="-7dp"
        android:background="@drawable/circle_red"
        android:visibility="gone">

        <TextView
            android:id="@+id/comment_icon_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textColor="@color/white"
            android:textSize="10sp"
            tools:text="3" />

    </FrameLayout>
</RelativeLayout>